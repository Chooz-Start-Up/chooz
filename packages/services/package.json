{
  "name": "@chooz/services",
  "version": "0.0.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc --build",
    "lint": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist *.tsbuildinfo"
  },
  "dependencies": {
    "@chooz/shared": "workspace:*",
    "firebase": "^11.0.0"
  },
  "devDependencies": {
    "typescript": "^5"
  }
}
